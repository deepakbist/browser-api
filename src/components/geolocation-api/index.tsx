import { Container, Typography, Box } from "@mui/material";

const GeoLocationAPI = () => {
  return (
    <Container>
      <Box>
        <Typography>Geolocation API</Typography>
      </Box>
    </Container>
  );
};

export default GeoLocationAPI;
